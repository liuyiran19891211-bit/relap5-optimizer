# 多目标控制系统参数优化平台设计计划

## 主要功能分析
当前系统已具备一个可用的 RELAP/控制系统参数优化闭环：

- 参数配置：支持从配置文件读取参数，按 `line_key + column_index` 写入 `indta.i`。
- 多目标定义：支持多个输出列、参考值、误差形式、权重和归一化尺度。
- 优化算法：已有黄金分割、贝叶斯优化、遗传算法 Pareto/NSGA-II 路线。
- 仿真执行：自动修改输入文件，运行 `start.bat`，检查 `outdta.o` 判断 RELAP 是否失败。
- 后处理：运行 `plot/strip.bat`，生成 CSV，计算时间积分目标。
- 运行监控：显示日志、心跳、评估历史、实时预览曲线。
- 结果分析：显示收敛曲线、参数搜索轨迹、Pareto 前沿、CSV/stripf 数据可视化。

## 功能面板设计
采用“左侧配置 + 顶部操作 + 中央四页签”的简洁布局，减少说明性文字，突出任务流。

- 侧边栏：运行环境、基准目录、配置加载、实时预览开关、当前运行状态。
- 顶部操作区：系统标题、当前配置文件、保存配置、启动优化、停止、会话状态。
- 页签 1：`参数与目标`
  - 参数表：名称、输入卡行号、列索引、初值、上下限、步长。
  - 目标表：输出列、参考值、误差类型、权重、scale。
  - CSV/stripf 路径和时间列配置。
- 页签 2：`算法与运行`
  - 优化器选择：遗传算法、贝叶斯、黄金分割。
  - 遗传算法参数：最大评估次数、随机种子、交叉概率、变异尺度、RELAP 失败重试次数。
  - 运行日志、实时进度、最近评估结果。
- 页签 3：`优化结果`
  - 最优参数、加权目标值、各子目标值。
  - Pareto 前沿表。
  - 收敛曲线、参数搜索轨迹、二维参数热图。
- 页签 4：`数据可视化`
  - Plotrec CSV 交互曲线。
  - stripf 原始 plotrec 曲线。
  - 支持选择横轴、纵轴、参考线。

视觉风格：浅色背景、少卡片、紧凑表格、关键按钮高亮；主要色只用于“启动优化”和最优/Pareto 状态，避免页面花哨。

## 开发计划
1. 梳理信息架构
   - 将现有 `配置工作台 / 运行与监视 / 优化轨迹 / 数据可视化` 重构为上面的四个业务页签。
   - 删除长说明块，把解释放进 tooltip 或 help 文案。

2. 强化多目标优化能力
   - 以遗传算法作为多目标默认算法。
   - 保留 `objective_values`、`objective_value`、`pareto_front`、`final_parameter_values` 作为标准结果结构。
   - Pareto 前沿中按加权目标选代表解写回 `indta.i`。

3. 优化运行体验
   - 运行中固定展示：当前评估编号、参数向量、RELAP 状态、失败重试次数、最新目标值。
   - 停止按钮只设置 stop flag，避免破坏当前文件状态。
   - 运行结束后自动展示最优解和 Pareto 前沿。

4. 优化结果分析
   - 增加 Pareto 前沿表的排序、下载和高亮代表解。
   - 参数轨迹支持多参数分面显示。
   - 二维热图用于任意两个参数组合的目标分布观察。

5. 收尾与验证
   - 增加配置校验：参数上下限、目标列索引、scale 非零、最大评估次数。
   - 增加单元测试覆盖配置生成、GA 返回结构、Pareto 前沿、UI 辅助函数。
   - 回归运行 `python -m unittest discover -s tests`。

## 接口与数据结构
保留并稳定以下字段：

- 输入配置：
  - `parameters[]`
  - `objectives[]`
  - `integral_objective`
  - `integral_optimizer`
- 每次评估记录：
  - `evaluation`
  - `parameter_values`
  - `objective_values`
  - `objective_value`
  - `relap_failed`
  - `retries`
- 最终结果：
  - `final_parameter_values`
  - `final_objective_values`
  - `final_objective_value`
  - `pareto_front`
  - `history`

## 测试计划
- 配置层：多参数、多目标、缺字段、非法上下限、scale 为 0。
- 算法层：GA 评估次数上限、失败点过滤、Pareto 前沿非空、代表解选择。
- 流程层：mock RELAP 后验证 `indta.i` 写入、history 记录、最终结果结构。
- UI 层：配置表单回收、优化器切换、Pareto 表展示、数据可视化 tab 可正常读取 CSV。

## 假设
- 继续基于现有 Streamlit 系统开发。
- RELAP 运行保持串行，不做并发评估。
- 遗传算法作为多目标默认优化器。
- 界面优先服务工程调参场景，风格简洁、信息密度适中。
